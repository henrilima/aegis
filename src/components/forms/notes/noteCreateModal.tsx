"use client"; import { FileText, Plus, X } from "lucide-react";
import { useState } from "react";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea"; interface NoteCreateModalProps { onAdd: (title: string, content: string) => void; onClose: () => void;
} export function NoteCreateModal({ onAdd, onClose }: NoteCreateModalProps) { const [title, setTitle] = useState(""); const [content, setContent] = useState(""); const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); if (!title.trim() || !content.trim()) return; onAdd(title.trim(), content.trim()); }; const ic = "bg-neutral-900 border-neutral-800 font-medium text-sm focus:border-orange-500/50 transition-all placeholder:text-neutral-600 "; const lc = "text-xs font-semibold text-neutral-400 ml-0.5"; return ( <div className="fixed inset-0 z-60 flex items-center justify-center p-4 bg-black/80 backdrop-blur-md animate-in fade-in duration-200" role="dialog" aria-modal="true" > <button type="button" aria-label="Fechar" className="absolute inset-0 w-full h-full cursor-default" onClick={onClose} /> <div className="relative w-full max-w-sm bg-neutral-950 border border-neutral-800 rounded-3xl animate-in zoom-in-95 duration-200 overflow-hidden"> {/* Cabeçalho */} <div className="flex items-center justify-between p-6 border-b border-neutral-800/50 "> <div className="flex items-center gap-3"> <div className="p-2.5 bg-orange-500/10 rounded-xl border border-orange-500/20"> <FileText className="w-5 h-5 text-orange-400" /> </div> <div> <h2 className="text-base font-bold text-white">Nova Nota</h2> <p className="text-xs text-neutral-500 mt-0.5">Captura de conhecimento</p> </div> </div> <button type="button" onClick={onClose} className="p-2 rounded-xl hover:bg-neutral-800 text-neutral-500 hover:text-white transition-all cursor-pointer" > <X className="w-5 h-5" /> </button> </div> <div className="max-h-[70vh] overflow-y-auto px-1 custom-scrollbar"> <form onSubmit={handleSubmit} className="p-6 space-y-4"> {/* Título */} <div className="space-y-1.5"> <Label htmlFor="ncm-title" className={lc}> Título </Label> <Input id="ncm-title" value={title} onChange={(e) => setTitle(e.target.value)} placeholder="Ex: Resumo de estudo, ideias de projeto..." className={`${ic} h-11 rounded-2xl`} autoFocus required /> </div> {/* Conteúdo */} <div className="space-y-1.5"> <Label htmlFor="ncm-content" className={lc}> Conteúdo <span className="text-neutral-600 font-normal">(Markdown)</span> </Label> <Textarea id="ncm-content" value={content} onChange={(e) => setContent(e.target.value)} placeholder="Escreva livremente aqui..." className={`${ic} rounded-2xl min-h-[160px] resize-none leading-relaxed pt-3`} required /> </div> {/* Ações */} <div className="flex flex-col gap-3 pt-2"> <button type="submit" disabled={!title.trim() || !content.trim()} className="w-full flex items-center justify-center gap-2.5 py-3.5 rounded-xl bg-orange-500/10 hover:bg-orange-500/20 border border-orange-500/60 hover:border-orange-400 text-orange-300 hover:text-orange-200 font-semibold text-sm transition-all active:scale-[0.98] cursor-pointer disabled:opacity-40" > <Plus className="w-4 h-4" /> Criar nota </button> <button type="button" onClick={onClose} className="w-full text-neutral-500 hover:text-neutral-300 py-2 text-sm font-medium cursor-pointer transition-colors" > Agora não </button> </div> </form> </div> </div> </div> );
}

